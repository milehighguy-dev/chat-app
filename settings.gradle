pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}


rootProject.name = 'chat-app'

// Include the backend subproject
include 'backend'

// Point to the backend directory
project(':backend').projectDir = file('backend')